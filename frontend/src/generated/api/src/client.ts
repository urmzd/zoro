// Auto-generated by oag — do not edit
import type {
  GraphData,
  NodeDetail,
  ResearchSession,
  SearchFactsResponse,
  SseEvent,
  StartResearchRequest,
  StartResearchResponse,
} from "./types";
import { type SSEOptions, streamSse } from "./sse";

/** Structured response wrapper exposing status, headers, and parsed data. */
export interface ApiResponse<T> {
  ok: boolean;
  status: number;
  statusText: string;
  headers: Headers;
  data: T;
}

/** Configuration for retry behavior with exponential backoff. */
export interface RetryConfig {
  /** Maximum number of retry attempts. Default: 3 */
  maxRetries?: number;
  /** Initial delay in milliseconds before the first retry. Default: 1000 */
  initialDelayMs?: number;
  /** Maximum delay in milliseconds between retries. Default: 30000 */
  maxDelayMs?: number;
  /** Multiplier applied to the delay after each retry. Default: 2 */
  backoffMultiplier?: number;
  /** HTTP status codes that trigger a retry. Default: [408, 429, 500, 502, 503, 504] */
  retryableStatusCodes?: number[];
  /** Whether to retry on network errors (fetch throws). Default: true */
  retryOnNetworkError?: boolean;
}

/** Options for API requests. */
export interface RequestOptions {
  signal?: AbortSignal;
  headers?: Record<string, string>;
  /** Per-request retry configuration. Set to false to disable retries. */
  retry?: RetryConfig | false;
  /** Per-request timeout in milliseconds. */
  timeout?: number;
}

/** Configuration for the API client. */
export interface ClientConfig {
  baseUrl: string;
  headers?: Record<string, string>;
  fetch?: typeof globalThis.fetch;
  requestInterceptor?: (request: { url: string; init: RequestInit }) =>
    { url: string; init: RequestInit } | Promise<{ url: string; init: RequestInit }>;
  /** Client-level retry configuration. Set to false to disable retries. Default: enabled with defaults. */
  retry?: RetryConfig | false;
  /** Client-level timeout in milliseconds. */
  timeout?: number;
}

/** Error thrown when an API request returns a non-OK status. */
export class ApiError extends Error {
  constructor(
    message: string,
    public readonly statusCode: number,
    public readonly statusText: string,
    public readonly body?: unknown,
  ) {
    super(message);
    this.name = "ApiError";
  }
}

const DEFAULT_RETRY_CONFIG: Required<RetryConfig> = {
  maxRetries: 3,
  initialDelayMs: 1000,
  maxDelayMs: 30000,
  backoffMultiplier: 2,
  retryableStatusCodes: [408, 429, 500, 502, 503, 504],
  retryOnNetworkError: true,
};

function mergeRetryConfig(
  clientLevel?: RetryConfig | false,
  requestLevel?: RetryConfig | false,
): Required<RetryConfig> | false {
  if (requestLevel === false) return false;
  if (clientLevel === false && requestLevel === undefined) return false;
  const base = clientLevel === false || clientLevel === undefined ? {} : clientLevel;
  const override = requestLevel ?? {};
  return { ...DEFAULT_RETRY_CONFIG, ...base, ...override };
}

function calculateBackoff(attempt: number, config: Required<RetryConfig>): number {
  const delay = config.initialDelayMs * Math.pow(config.backoffMultiplier, attempt);
  const capped = Math.min(delay, config.maxDelayMs);
  const jitter = capped * 0.25 * Math.random();
  return capped + jitter;
}

function parseRetryAfter(headers: Headers): number | undefined {
  const value = headers.get("retry-after");
  if (value === null) return undefined;
  const seconds = Number(value);
  if (!Number.isNaN(seconds)) return seconds * 1000;
  const date = Date.parse(value);
  if (!Number.isNaN(date)) return Math.max(0, date - Date.now());
  return undefined;
}

function sleep(ms: number, signal?: AbortSignal): Promise<void> {
  return new Promise((resolve, reject) => {
    if (signal?.aborted) {
      reject(signal.reason ?? new DOMException("Aborted", "AbortError"));
      return;
    }
    const timer = setTimeout(resolve, ms);
    signal?.addEventListener("abort", () => {
      clearTimeout(timer);
      reject(signal.reason ?? new DOMException("Aborted", "AbortError"));
    }, { once: true });
  });
}

function buildFormData(body: Record<string, unknown>): FormData {
  const formData = new FormData();
  for (const [key, value] of Object.entries(body)) {
    if (value === undefined || value === null) continue;
    if (value instanceof Blob) {
      formData.append(key, value, value instanceof File ? value.name : undefined);
    } else if (Array.isArray(value)) {
      for (const item of value) {
        if (item instanceof Blob) {
          formData.append(key, item, item instanceof File ? item.name : undefined);
        } else if (typeof item === "object") {
          formData.append(key, JSON.stringify(item));
        } else {
          formData.append(key, String(item));
        }
      }
    } else if (typeof value === "object") {
      formData.append(key, JSON.stringify(value));
    } else {
      formData.append(key, String(value));
    }
  }
  return formData;
}

/** API client for Zoro API. */
export class ApiClient {
  private readonly baseUrl: string;
  private readonly headers: Record<string, string>;
  private readonly fetchFn: typeof globalThis.fetch;
  private readonly requestInterceptor?: ClientConfig["requestInterceptor"];
  private readonly retryConfig?: RetryConfig | false;
  private readonly timeout?: number;

  constructor(config: ClientConfig) {
    this.baseUrl = config.baseUrl.replace(/\/$/, "");
    this.headers = config.headers ?? {};
    this.fetchFn = config.fetch ?? globalThis.fetch.bind(globalThis);
    this.requestInterceptor = config.requestInterceptor;
    this.retryConfig = config.retry;
    this.timeout = config.timeout;
  }

  private async rawRequest<T>(
    method: string,
    path: string,
    options?: RequestOptions & {
      body?: unknown;
      query?: Record<string, unknown>;
      contentType?: string;
      isMultipart?: boolean;
    },
  ): Promise<ApiResponse<T>> {
    let url = `${this.baseUrl}${path}`;
    if (options?.query) {
      const params = new URLSearchParams();
      for (const [key, value] of Object.entries(options.query)) {
        if (value !== undefined && value !== null) {
          if (Array.isArray(value)) {
            for (const v of value) {
              params.append(key, String(v));
            }
          } else if (typeof value === "object") {
            for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
              if (v !== undefined && v !== null) params.set(`${key}[${k}]`, String(v));
            }
          } else {
            params.set(key, String(value));
          }
        }
      }
      const qs = params.toString();
      if (qs) url += `?${qs}`;
    }

    const hasBody = options?.body !== undefined;
    const isMultipart = options?.isMultipart === true;
    const contentType = options?.contentType ?? "application/json";

    let serializedBody: BodyInit | undefined;
    if (hasBody) {
      if (isMultipart) {
        serializedBody = buildFormData(options!.body as Record<string, unknown>);
      } else if (contentType === "application/json") {
        serializedBody = JSON.stringify(options!.body);
      } else {
        serializedBody = options!.body as BodyInit;
      }
    }

    // For multipart, do NOT set Content-Type — fetch sets it with the boundary automatically
    const headers: Record<string, string> = {
      ...(hasBody && !isMultipart ? { "Content-Type": contentType } : {}),
      ...this.headers,
      ...options?.headers,
    };

    let req = {
      url,
      init: {
        method,
        headers,
        body: serializedBody,
        signal: options?.signal,
      } as RequestInit,
    };

    if (this.requestInterceptor) {
      req = await this.requestInterceptor(req);
    }

    // Build timeout signal if configured
    const requestTimeout = options?.timeout ?? this.timeout;
    let timeoutSignal: AbortSignal | undefined;
    if (requestTimeout !== undefined) {
      timeoutSignal = AbortSignal.timeout(requestTimeout);
    }

    // Combine user signal and timeout signal
    const combinedSignal = req.init.signal && timeoutSignal
      ? AbortSignal.any([req.init.signal, timeoutSignal])
      : timeoutSignal ?? req.init.signal;

    if (combinedSignal) {
      req.init.signal = combinedSignal;
    }

    // Retry logic
    const retryConfig = mergeRetryConfig(this.retryConfig, options?.retry);

    if (retryConfig === false) {
      return this.executeFetch<T>(req);
    }

    let lastError: unknown;
    for (let attempt = 0; attempt <= retryConfig.maxRetries; attempt++) {
      try {
        const response = await this.executeFetch<T>(req);
        if (response.ok || attempt === retryConfig.maxRetries) {
          return response;
        }
        if (!retryConfig.retryableStatusCodes.includes(response.status)) {
          return response;
        }
        const retryAfter = parseRetryAfter(response.headers);
        const backoff = retryAfter ?? calculateBackoff(attempt, retryConfig);
        await sleep(backoff, options?.signal);
      } catch (error) {
        lastError = error;
        if (!retryConfig.retryOnNetworkError || attempt === retryConfig.maxRetries) {
          throw error;
        }
        // Don't retry abort errors
        if (error instanceof DOMException && error.name === "AbortError") {
          throw error;
        }
        const backoff = calculateBackoff(attempt, retryConfig);
        await sleep(backoff, options?.signal);
      }
    }

    throw lastError;
  }

  private async executeFetch<T>(req: { url: string; init: RequestInit }): Promise<ApiResponse<T>> {
    const response = await this.fetchFn(req.url, req.init);

    let data: T;
    if (response.status === 204) {
      data = undefined as T;
    } else {
      const text = await response.text();
      if (text) {
        try {
          data = JSON.parse(text) as T;
        } catch {
          data = text as T;
        }
      } else {
        data = undefined as T;
      }
    }

    return {
      ok: response.ok,
      status: response.status,
      statusText: response.statusText,
      headers: response.headers,
      data,
    };
  }

  private async request<T>(
    method: string,
    path: string,
    options?: RequestOptions & {
      body?: unknown;
      query?: Record<string, unknown>;
      contentType?: string;
      isMultipart?: boolean;
    },
  ): Promise<T> {
    const response = await this.rawRequest<T>(method, path, options);
    if (!response.ok) {
      throw new ApiError(
        `API request failed: ${response.status} ${response.statusText}`,
        response.status,
        response.statusText,
        response.data,
      );
    }
    return response.data;
  }

  /** Health check */
  async healthCheck(options?: RequestOptions): Promise<Record<string, string>> {
    const path = "/health";
    return this.request<Record<string, string>>("GET", path, {
      ...options,
    });
  }

  async healthCheckRaw(options?: RequestOptions): Promise<ApiResponse<Record<string, string>>> {
    const path = "/health";
    return this.rawRequest<Record<string, string>>("GET", path, {
      ...options,
    });
  }

  /** Creates a new research session for the given query */
  async startResearch(body: StartResearchRequest, options?: RequestOptions): Promise<StartResearchResponse> {
    const path = "/api/research";
    return this.request<StartResearchResponse>("POST", path, {
      body,
      contentType: "application/json",
      ...options,
    });
  }

  async startResearchRaw(body: StartResearchRequest, options?: RequestOptions): Promise<ApiResponse<StartResearchResponse>> {
    const path = "/api/research";
    return this.rawRequest<StartResearchResponse>("POST", path, {
      body,
      contentType: "application/json",
      ...options,
    });
  }

  /** Returns the research session with the given ID */
  async getResearchSession(id: string, options?: RequestOptions): Promise<ResearchSession> {
    let path = "/api/research/{id}";
    path = path.replace("{id}", encodeURIComponent(String(id)));
    return this.request<ResearchSession>("GET", path, {
      ...options,
    });
  }

  async getResearchSessionRaw(id: string, options?: RequestOptions): Promise<ApiResponse<ResearchSession>> {
    let path = "/api/research/{id}";
    path = path.replace("{id}", encodeURIComponent(String(id)));
    return this.rawRequest<ResearchSession>("GET", path, {
      ...options,
    });
  }

  /** Returns an SSE stream of research progress events */
  async *streamResearchEvents(id: string, options?: SSEOptions): AsyncGenerator<SseEvent> {
    let path = "/api/research/{id}/stream";
    path = path.replace("{id}", encodeURIComponent(String(id)));
    let url = `${this.baseUrl}${path}`;
    yield* streamSse<SseEvent>(url, {
      method: "GET",
      headers: { ...this.headers, ...options?.headers },
    }, options, this.requestInterceptor, this.fetchFn);
  }

  /** Searches for facts in the knowledge graph */
  async searchKnowledge(q: string, options?: RequestOptions): Promise<SearchFactsResponse> {
    let path = "/api/knowledge/search";
    const _q = new URLSearchParams();
    if (q !== undefined && q !== null) _q.set("q", String(q));
    const _qs = _q.toString();
    if (_qs) path += `?${_qs}`;
    return this.request<SearchFactsResponse>("GET", path, {
      ...options,
    });
  }

  async searchKnowledgeRaw(q: string, options?: RequestOptions): Promise<ApiResponse<SearchFactsResponse>> {
    let path = "/api/knowledge/search";
    const _q = new URLSearchParams();
    if (q !== undefined && q !== null) _q.set("q", String(q));
    const _qs = _q.toString();
    if (_qs) path += `?${_qs}`;
    return this.rawRequest<SearchFactsResponse>("GET", path, {
      ...options,
    });
  }

  /** Returns nodes and edges from the knowledge graph */
  async getKnowledgeGraph(limit?: number, options?: RequestOptions): Promise<GraphData> {
    let path = "/api/knowledge/graph";
    const _q = new URLSearchParams();
    if (limit !== undefined && limit !== null) _q.set("limit", String(limit));
    const _qs = _q.toString();
    if (_qs) path += `?${_qs}`;
    return this.request<GraphData>("GET", path, {
      ...options,
    });
  }

  async getKnowledgeGraphRaw(limit?: number, options?: RequestOptions): Promise<ApiResponse<GraphData>> {
    let path = "/api/knowledge/graph";
    const _q = new URLSearchParams();
    if (limit !== undefined && limit !== null) _q.set("limit", String(limit));
    const _qs = _q.toString();
    if (_qs) path += `?${_qs}`;
    return this.rawRequest<GraphData>("GET", path, {
      ...options,
    });
  }

  /** Returns a node and its neighbors from the knowledge graph */
  async getNodeDetail(id: string, depth?: number, options?: RequestOptions): Promise<NodeDetail> {
    let path = "/api/knowledge/node/{id}";
    path = path.replace("{id}", encodeURIComponent(String(id)));
    const _q = new URLSearchParams();
    if (depth !== undefined && depth !== null) _q.set("depth", String(depth));
    const _qs = _q.toString();
    if (_qs) path += `?${_qs}`;
    return this.request<NodeDetail>("GET", path, {
      ...options,
    });
  }

  async getNodeDetailRaw(id: string, depth?: number, options?: RequestOptions): Promise<ApiResponse<NodeDetail>> {
    let path = "/api/knowledge/node/{id}";
    path = path.replace("{id}", encodeURIComponent(String(id)));
    const _q = new URLSearchParams();
    if (depth !== undefined && depth !== null) _q.set("depth", String(depth));
    const _qs = _q.toString();
    if (_qs) path += `?${_qs}`;
    return this.rawRequest<NodeDetail>("GET", path, {
      ...options,
    });
  }
}
